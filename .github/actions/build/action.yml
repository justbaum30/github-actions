name: 'Build'
description: 'Run Gradle build and upload artifact results'
runs:
  using: composite
  steps:
    - name: Build
      run: ./gradlew bootJar
    - name: Upload Artifacts
      uses: actions/upload-artifact@v2.2.4
      with:
        name: Artifacts
        path: build/libs/**
